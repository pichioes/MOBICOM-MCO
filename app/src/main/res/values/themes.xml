<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Splash Theme without ActionBar -->
    <style name="Theme.MOBICOMMCO2.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowSplashScreenAnimationDuration">1900</item>
        <item name="postSplashScreenTheme">@style/Theme.MOBICOMMCO2</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Main App Theme without ActionBar -->
    <style name="Theme.MOBICOMMCO2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
    </style>

</resources>
